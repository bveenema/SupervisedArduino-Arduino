syntax = "proto3";

enum SuperLogLevel {
    unspecified = 0;
    debug = 1;
    info = 2;
    warn = 3;
    error = 4;
    fatal = 5;
}

message SuperLogMessage {
    SuperLogLevel level = 1;
    string message = 2;
}